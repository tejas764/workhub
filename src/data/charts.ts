export const workloadData: { month: string; hours: number }[] = [];

export const taskTrendData: { month: string; completed: number; pending: number }[] = [];

export const uploadTrendData: { month: string; docs: number }[] = [];

export const meetingData: { month: string; meetings: number }[] = [];

export const deptDistData: { name: string; value: number }[] = [];
